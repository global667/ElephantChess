#include "engine.h"

engine::engine()
{
    
}

Position engine::getBestMove(Color color)
{
    return Position();
}

QList<Position> engine::getPossibleMoves(Piece *piece)
{
    return QList<Position>();
}

QList<Position> engine::getPossibleMoves(Color color)
{
    return QList<Position>();
}
